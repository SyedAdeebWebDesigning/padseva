import NewsletterForm from "@/components/shared/NewsletterForm";

interface pageProps {}

const page = ({}: pageProps) => {
	return <div>
        <NewsletterForm />
    </div>;
};

export default page;
